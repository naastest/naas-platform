# ArgoCD cluster secret for the PRODUCTION cluster.
apiVersion: v1
kind: Secret
metadata:
  name: cluster-production
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: cluster
    naas.io/managed: "true"
    naas.io/env: production
type: Opaque
stringData:
  name: production
  server: https://production-cluster.example.com   # TODO: replace with real cluster URL
  config: |
    {
      "bearerToken": "REPLACE_WITH_SEALED_SECRET",
      "tlsClientConfig": {
        "insecure": false,
        "caData": "REPLACE_WITH_BASE64_CA"
      }
    }
