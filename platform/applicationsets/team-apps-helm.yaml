# Helm-based team workloads — alternative to raw-manifest apps/dev/.
#
# Teams put per-service values files under apps/helm/{env}/ in their team repo.
# Each file is a standard Helm values file for the naastest/helm-charts `service`
# chart. The ApplicationSet reads the files via the Git generator and creates one
# Helm Application per file per environment.
#
# File layout expected in each team repo:
#   apps/helm/dev/*.yaml          → deployed to dev cluster
#   apps/helm/test/*.yaml         → deployed to test cluster
#   apps/helm/acceptance/*.yaml   → deployed to acceptance cluster
#   apps/helm/production/*.yaml   → deployed to production cluster
#
# Required keys in each values file:
#   image          full image:tag string
#   containerPort  port the container listens on
#   namespace      target Kubernetes namespace  (must match {team}-*)
#   resources      requests + limits (required by Kyverno)
#
# The release name (and all Kubernetes resource names) is derived from the
# values file stem: apps/helm/dev/checkout-service.yaml → "checkout-service".
#
# Chart version is pinned here. Bump it in all four ApplicationSets when
# releasing a new chart version.
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: tenant-apps-helm-dev
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - matrix:
        generators:
          - scmProvider:
              github:
                organization: naastest
                tokenRef:
                  secretName: argocd-scm-token
                  key: token
              filters:
                - repositoryMatch: "^team-"
                  branchMatch: "^main$"
                  pathsExist: ["apps/helm/dev"]
              cloneProtocol: https
          - git:
              repoURL: "{{.url}}"
              revision: "{{.branch}}"
              files:
                - path: "apps/helm/dev/*.yaml"
  template:
    metadata:
      name: "helm-{{trimPrefix `team-` .repository}}-{{.path.filename | trimSuffix `.yaml`}}-dev"
      labels:
        naas.io/managed-by: helm-applicationset
        naas.io/env: dev
    spec:
      project: '{{trimPrefix "team-" .repository}}'
      source:
        repoURL: "https://github.com/naastest/helm-charts"
        chart: service
        targetRevision: "0.1.0"
        helm:
          releaseName: '{{.path.filename | trimSuffix ".yaml"}}'
          valuesObject:
            image: "{{.image}}"
            containerPort: '{{.containerPort | default "8080"}}'
            replicaCount: '{{.replicaCount | default "1"}}'
            resources: "{{.resources}}"
            probes: '{{.probes | default "{}"}}'
            hpa: '{{.hpa | default "{}"}}'
            pdb: '{{.pdb | default "{}"}}'
            ingress: '{{.ingress | default "{}"}}'
            env: '{{.env | default "{}"}}'
      destination:
        name: dev
        namespace: "{{.namespace}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - ServerSideApply=true
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: tenant-apps-helm-test
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - matrix:
        generators:
          - scmProvider:
              github:
                organization: naastest
                tokenRef:
                  secretName: argocd-scm-token
                  key: token
              filters:
                - repositoryMatch: "^team-"
                  branchMatch: "^main$"
                  pathsExist: ["apps/helm/test"]
              cloneProtocol: https
          - git:
              repoURL: "{{.url}}"
              revision: "{{.branch}}"
              files:
                - path: "apps/helm/test/*.yaml"
  template:
    metadata:
      name: "helm-{{trimPrefix `team-` .repository}}-{{.path.filename | trimSuffix `.yaml`}}-test"
      labels:
        naas.io/managed-by: helm-applicationset
        naas.io/env: test
    spec:
      project: '{{trimPrefix "team-" .repository}}'
      source:
        repoURL: "https://github.com/naastest/helm-charts"
        chart: service
        targetRevision: "0.1.0"
        helm:
          releaseName: '{{.path.filename | trimSuffix ".yaml"}}'
          valuesObject:
            image: "{{.image}}"
            containerPort: '{{.containerPort | default "8080"}}'
            replicaCount: '{{.replicaCount | default "1"}}'
            resources: "{{.resources}}"
            probes: '{{.probes | default "{}"}}'
            hpa: '{{.hpa | default "{}"}}'
            pdb: '{{.pdb | default "{}"}}'
            ingress: '{{.ingress | default "{}"}}'
            env: '{{.env | default "{}"}}'
      destination:
        name: test
        namespace: "{{.namespace}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - ServerSideApply=true
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: tenant-apps-helm-acceptance
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - matrix:
        generators:
          - scmProvider:
              github:
                organization: naastest
                tokenRef:
                  secretName: argocd-scm-token
                  key: token
              filters:
                - repositoryMatch: "^team-"
                  branchMatch: "^main$"
                  pathsExist: ["apps/helm/acceptance"]
              cloneProtocol: https
          - git:
              repoURL: "{{.url}}"
              revision: "{{.branch}}"
              files:
                - path: "apps/helm/acceptance/*.yaml"
  template:
    metadata:
      name: "helm-{{trimPrefix `team-` .repository}}-{{.path.filename | trimSuffix `.yaml`}}-acceptance"
      labels:
        naas.io/managed-by: helm-applicationset
        naas.io/env: acceptance
    spec:
      project: '{{trimPrefix "team-" .repository}}'
      source:
        repoURL: "https://github.com/naastest/helm-charts"
        chart: service
        targetRevision: "0.1.0"
        helm:
          releaseName: '{{.path.filename | trimSuffix ".yaml"}}'
          valuesObject:
            image: "{{.image}}"
            containerPort: '{{.containerPort | default "8080"}}'
            replicaCount: '{{.replicaCount | default "1"}}'
            resources: "{{.resources}}"
            probes: '{{.probes | default "{}"}}'
            hpa: '{{.hpa | default "{}"}}'
            pdb: '{{.pdb | default "{}"}}'
            ingress: '{{.ingress | default "{}"}}'
            env: '{{.env | default "{}"}}'
      destination:
        name: acceptance
        namespace: "{{.namespace}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - ServerSideApply=true
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: tenant-apps-helm-production
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - matrix:
        generators:
          - scmProvider:
              github:
                organization: naastest
                tokenRef:
                  secretName: argocd-scm-token
                  key: token
              filters:
                - repositoryMatch: "^team-"
                  branchMatch: "^main$"
                  pathsExist: ["apps/helm/production"]
              cloneProtocol: https
          - git:
              repoURL: "{{.url}}"
              revision: "{{.branch}}"
              files:
                - path: "apps/helm/production/*.yaml"
  template:
    metadata:
      name: "helm-{{trimPrefix `team-` .repository}}-{{.path.filename | trimSuffix `.yaml`}}-production"
      labels:
        naas.io/managed-by: helm-applicationset
        naas.io/env: production
    spec:
      project: '{{trimPrefix "team-" .repository}}'
      source:
        repoURL: "https://github.com/naastest/helm-charts"
        chart: service
        targetRevision: "0.1.0"
        helm:
          releaseName: '{{.path.filename | trimSuffix ".yaml"}}'
          valuesObject:
            image: "{{.image}}"
            containerPort: '{{.containerPort | default "8080"}}'
            replicaCount: '{{.replicaCount | default "1"}}'
            resources: "{{.resources}}"
            probes: '{{.probes | default "{}"}}'
            hpa: '{{.hpa | default "{}"}}'
            pdb: '{{.pdb | default "{}"}}'
            ingress: '{{.ingress | default "{}"}}'
            env: '{{.env | default "{}"}}'
      destination:
        name: production
        namespace: "{{.namespace}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - ServerSideApply=true
